# Java Manager

The daemon half owns Java runtime storage under `data/JavaData`, download and
installation, the `java_manager/*` protocol events and the `{mcsm_java}`
command expansion used by instances.
