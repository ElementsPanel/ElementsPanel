# Java Manager

The panel half of the Java Manager plugin owns the `/api/java_manager` HTTP
routes and the Java runtime action shown in normal and Desktop instance
terminals. The daemon must also have its `java` plugin enabled for the
action to appear for a node.
