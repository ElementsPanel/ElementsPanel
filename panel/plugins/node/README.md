# Node Management

Provides the regular node management pages, Docker image management cards, node overview cards, and the node manager window in Desktop mode.

The plugin uses the panel's shared node APIs and remote-node hooks. It registers its routes and layout cards at runtime, so disabling or removing the plugin removes the associated pages and cards as well.
